//! Mirrorscope CLI — `mirrorscope record|replay|dap` once those layers land.

fn main() {
    println!("mirrorscope {}", env!("CARGO_PKG_VERSION"));
    println!("pre-implementation scaffold; see README.md §8 for the build order");
}
